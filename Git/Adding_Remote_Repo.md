
The command to add a remote repository is:

```bash
git remote add <name> <url>
```

Examples:
```bash
git remote add origin https://github.com/user/repo.git
git remote add main https://github.com/user/repo.git
```

You can verify the remotes with:
```bash
git remote -v
```